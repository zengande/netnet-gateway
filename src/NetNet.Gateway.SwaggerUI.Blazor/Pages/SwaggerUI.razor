@page "/SwaggerUI/{DocumentName}"
@using Swashbuckle.AspNetCore.Swagger
@using System.Text.Json

@inject ISwaggerProvider SwaggerProvider;


<code>
    @JsonSerializer.Serialize(SwaggerProvider.GetSwagger(DocumentName))
</code>

@code {

    [Parameter]
    public string DocumentName { get; set; }

}
